[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "avap_lite"
version = "0.1.0"
description = "High-performance AVAP execution engine"
requires-python = ">=3.11"
dependencies = [
    "tornado>=6.3",
    "asyncpg>=0.28",
    "redis>=5.0"
]
